# sharp 0.34 only reads SHARP_IGNORE_GLOBAL_LIBVIPS, not npmrc keys.
# pnpm skips sharp's compile via allowBuilds.sharp=false in pnpm-workspace.yaml.
# create-zstack sets the env var for the first install (npm/yarn included).
